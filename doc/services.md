## BaseArticle
```
// 返回创建的对象
Article function create(Object content, List tagNames);

// 返回删除的数据的ID
Number function delete(Number id);

// 
??? function update(Number id, Object data);

List function queryList(Number page, Number Size);

Article function query(Number id);
```

